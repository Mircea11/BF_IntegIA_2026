[project]
name = "deeplearning"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "polars>=1.44.1",
]

[project.scripts]
deeplearning = "deeplearning:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"

[tool.uv.workspace]
members = [
    "deeplearning/deeplearning",
    "deeplearning1",
]
