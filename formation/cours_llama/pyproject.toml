[project]
name = "cours-llama"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Mircea Bordeianu~", email = "m.bordeianu@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "ipykernel>=7.3.0",
    "openai>=3.15.0",
    "polars>=1.44.2",
    "requests>=2.34.2",
]

[project.scripts]
cours-llama = "cours_llama:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
